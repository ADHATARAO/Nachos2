#ifdef CHANGED

#include "thread.h"
#include "synch.h"


extern int do_UserThreadCreate(int f);// fonction appelé par l'apl systeme 
static void StartUserThread(int f);// fonction local utilisé par do_UserThreadCreate
void do_UserThreadExit();// Exit un thread 
void do_UserThreadJoin(int idThread);




#endif // CHANGED
